{
  "name": "wooftrade",
  "version": "0.0.3",
  "description": "wooftrade CLI tool",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "bin": "./dist/index.js",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc",
    "dev": "ts-node src/index.ts",
    "start": "node dist/index.js",
    "clean": "rm -rf dist",
    "prepublishOnly": "yarn clean && yarn build",
    "publish:live": "yarn prepublishOnly && yarn npm publish --access public",
    "test": "vitest run --exclude 'dist/**'",
    "format": "prettier --write .",
    "format:check": "prettier --check ."
  },
  "keywords": [],
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/wooftrade/wooftrade-cli"
  },
  "packageManager": "yarn@4.12.0",
  "devDependencies": {
    "@types/node": "^25.3.2",
    "prettier": "^3.8.1",
    "ts-node": "^10.9.2",
    "typescript": "^5.9.3",
    "vitest": "^4.0.18"
  },
  "dependencies": {
    "@1inch/fusion-sdk": "^2.4.6",
    "axios": "^1.13.6",
    "commander": "^14.0.3",
    "viem": "^2.46.3"
  }
}
